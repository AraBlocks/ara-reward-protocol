syntax = "proto3";
package routeguide;
import "messages.proto";

service RFP {
  rpc RequestQuote(messages.SOW) returns(messages.Quote);
  rpc AwardContract(messages.Contract) returns(messages.Contract);
  rpc DeliverReward(messages.Reward) returns(messages.ARAid);
}
