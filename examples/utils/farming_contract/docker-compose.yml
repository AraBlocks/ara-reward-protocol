version: '1'
services:
  ganachecli:
    image: trufflesuite/ganache-cli:latest
    command: "--seed littlstar -h 0.0.0.0"
    ports:
      - "8545:8545"
  truffle:
    image: hdao/contract-migration
    command: truffle migrate
    volumes:
      - ./:/code
